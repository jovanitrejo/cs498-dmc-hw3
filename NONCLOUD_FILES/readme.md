The following directory contains all files used locally for this homework assignment, which included loading the data using a python script into mongodb, and the python etl used to compare api times.

Also includes the `requirements.txt` file with the packages used for it.